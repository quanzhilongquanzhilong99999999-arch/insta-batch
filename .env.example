# Copy to .env (gitignored) and fill real values.

# Token for the proxy provider API referenced in config/settings.yaml
PROXY_API_TOKEN=your-proxy-api-token-here

# Optional: override log level for a one-off run
# LOG_LEVEL=DEBUG
